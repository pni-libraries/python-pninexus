A short developer not on testing IO
===================================

This file is authorative for fields and attributes (as they are now supporting
the same IO interface). 

We can read and write scalar or multidimensional data. This can either be done 
    
Possible scenarios 
    memory    nexus
    scalar -> scalar 
    scalar <- scalar
    scalar -> array broadcast
    scalar -> array partial
    
    array  -> array (same shape)
    array  -> array (partial io)
    array  <- array (same shape)
    array  <- array (partial io)


